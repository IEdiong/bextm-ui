import { FilterButtonDirective } from './filter-button.directive';

describe('FilterButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterButtonDirective();
    expect(directive).toBeTruthy();
  });
});
