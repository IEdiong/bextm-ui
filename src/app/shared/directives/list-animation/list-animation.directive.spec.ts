import { ListAnimationDirective } from './list-animation.directive';

describe('ListAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ListAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
